# 세일즈맵 — 배포 체크리스트 (웹 배포 → Google Play)

이 앱은 PWA(설치형 웹앱)입니다. 순서는 **① 웹(HTTPS) 배포 → ② Play Store 패키징**입니다.
(Chrome 웹스토어는 2020년부터 PWA를 받지 않으므로 대상이 아닙니다.)

---

## STEP 1. 웹(HTTPS)에 먼저 배포 — 필수, 무료

Play Store 패키징은 "실제 HTTPS 주소"가 있어야 가능합니다. 먼저 웹에 올립니다.

- [ ] 무료 호스팅 택 1: **Netlify / Vercel / Cloudflare Pages / GitHub Pages**
- [ ] 이 폴더(`세일즈맵_모바일`) 전체를 업로드 (드래그&드롭 또는 Git 연결)
- [ ] 배포 후 주소 확인 예: `https://내프로젝트.netlify.app/index.html`
- [ ] **HTTPS 필수** — 서비스워커·GPS·공유·카메라는 HTTP에서 동작하지 않음
- [ ] 브라우저에서 열어 "홈 화면에 추가"가 뜨는지, 오프라인에서 열리는지 확인
- [ ] `privacy.html`도 함께 올라갔는지 확인 → 주소 예: `https://내프로젝트.netlify.app/privacy.html`

> 사내/개인 사용만 목적이라면 여기까지만 해도 충분합니다. 링크만 공유하면 각자 설치해서 씁니다.

---

## STEP 2. Google Play 스토어 등록 (안드로이드)

### 2-1. 사전 준비물
- [ ] Google Play Console 개발자 계정 (**최초 1회 $25**)
- [ ] STEP 1의 HTTPS 주소
- [ ] 개인정보처리방침 URL (`.../privacy.html`) — **사진·위치 사용으로 필수**
- [ ] 앱 아이콘 512×512 (이미 `icon-512.png` 보유)
- [ ] 스크린샷 최소 2장 (폰 실제 화면 캡처) + 기능 그래픽 1024×500

### 2-2. 설치용 패키지(AAB) 만들기 — PWABuilder 사용
- [ ] [https://www.pwabuilder.com](https://www.pwabuilder.com) 접속 → STEP 1 주소 입력
- [ ] 리포트에서 manifest/서비스워커 통과 확인
- [ ] **Package For Stores → Android(Google Play)** 선택 → AAB 다운로드
- [ ] 함께 제공되는 **`assetlinks.json`** 파일을 받아둔다

### 2-3. 도메인 검증 (Digital Asset Links) — 주소창 숨김에 필수
- [ ] 받은 `assetlinks.json`을 웹 서버의 **`/.well-known/assetlinks.json`** 경로에 올린다
- [ ] `https://내주소/.well-known/assetlinks.json` 이 열리는지 확인
- [ ] (안 하면 앱 상단에 브라우저 주소창이 남습니다)

### 2-4. Play Console 등록
- [ ] 앱 만들기 → 이름 "세일즈맵", 언어 한국어, 무료
- [ ] 프로덕션 → AAB 업로드
- [ ] 스토어 등록정보: 간단설명/자세한설명/스크린샷/아이콘/기능그래픽
- [ ] 개인정보처리방침 URL 입력
- [ ] 콘텐츠 등급 설문, 타겟 연령, 광고 포함 여부(→ **없음**) 작성
- [ ] 데이터 보안(Data safety) 설문 작성 → 아래 2-5 참고
- [ ] 심사 제출 (보통 수 시간~며칠)

### 2-5. 데이터 보안(Data safety) 설문 답변 가이드
이 앱 구조(서버 없음, 전부 기기 저장)에 맞춘 권장 답변입니다.

| 항목 | 답변 |
|---|---|
| 데이터를 수집/전송하나요? | **아니오** (모든 데이터가 기기 내에만 저장, 서버 전송 없음) |
| 위치정보 | 앱 기능(주소 변환)에 사용하나 서버로 수집·저장하지 않음 → "수집 안 함"으로 처리 가능. 단 위치 **권한**은 요청함 |
| 사진/미디어 | 기기 내 저장만, 전송 안 함 → "수집 안 함" |
| 데이터 암호화(전송 중) | 해당 없음(전송 안 함), 지오코딩 호출은 HTTPS |
| 데이터 삭제 요청 방법 | 앱 내 "전체 데이터 초기화" 제공 |

> 주의: 위치를 서버로 보내 저장한다면 "수집함"으로 바꿔야 합니다. 현재는 좌표를
> 주소 변환에만 일시 사용하고 저장하지 않으므로 위와 같이 답합니다.

### 2-6. 권한 사용 사유 (심사 문의 대비 문구)
- [ ] **위치(GPS)**: "방문 업체 주소를 현재 위치로 자동 입력하기 위해 사용자가 버튼을 누른 경우에만 사용"
- [ ] **카메라/사진**: "방문 현장 사진을 방문일지에 첨부하기 위해 사용"

---

## STEP 3. (선택) Apple App Store
- [ ] Apple Developer 계정 $99/년 + Mac + Xcode 필요
- [ ] PWABuilder에서 iOS 패키지 생성 후 Xcode로 빌드·업로드
- [ ] Apple은 단순 웹뷰 래핑 앱 심사가 까다로움 → 오프라인·사진·위치 등
      네이티브적 가치를 설명해야 반려 위험이 줄어듭니다

---

## 배포 전 최종 점검
- [ ] 데이터관리 → "백업 파일 다운로드"로 기존 데이터 백업 (신버전 첫 실행 시 사진 마이그레이션 발생)
- [ ] 실제 안드로이드/아이폰에서: 저장 · 사진첨부 · 백업/복원 · 인쇄 · GPS 동작 확인
- [ ] 서비스워커 캐시 버전(`sw-mobile.js`의 CACHE_NAME) 올렸는지 확인 (현재 v28)
